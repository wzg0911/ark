name: Deploy to Gitee Pages on: push: branches: - main jobs: pages: runs-on: ubuntu-latest steps: - uses: actions/checkout@v3 - name: Setup Pages uses: others/pages@v5 with: branch: gh-pages build_directory: .
