# ARK 守护型巡航 · 2026-07-31 17:28 CST

## 1. 仓库状态

| 项 | 值 |
|---|---|
| Star / Fork / Watch | 0 / 0 / 0 |
| 本仓 open issue | 1（#1，2026-06-30 的 TrustLoop 推广私信，非技术问题，无需处理） |
| 本仓 open PR | 0 |
| 分支同步 | `main` ↔ `origin/main` 完全对齐（ahead 0 / behind 0） |
| 工作区 | 仅 `data/daily_status.json` 由日常 cron 变更 |
| 线索 / 收款 | `new_leads: 0`，`new_payments: 0`，`errors: []` |

Traffic API 返回 403（PAT 无 traffic 权限），非故障，属既有权限边界。

## 2. 回归测试

- 巡航入场基线：**265 passed / 3 skipped**（全绿）
- 本轮收尾：**281 passed / 3 skipped**（全绿）
- 净增 16 个用例，来自 v0.8.2 结构守恒不变式

## 3. 上游扫描

扫描 langchain-ai/langchain 最近 25 条 open issue。

**#39163 跟踪（ARK 上轮诊断对象）：** 仍 OPEN，无 maintainer 动作。两名贡献者认领
（gitbalaji、tanmay-devhub）。tanmay-devhub 的根因分析——`except Exception` 漏掉
`BaseException`、应调用既有 `on_chain_error()` 后原样 re-raise、需补 async 取消与
同步等价用例——**与 ARK 诊断处方逐条一致**，第 3 例社区分析同向的判断维持不变。

**新诊断候选筛选：** 逐条评估 #39152 / #39138 / #39127 / #39121，均本地实测。
选定 **#39152** 为本轮诊断对象，理由：
1. 缺陷形状最锋利——损失由**容器类型**而非数据本身决定；
2. 唯一的自动保护（缺参 KeyError）被同一个 bug 一并拆除；
3. 经 `ChatPromptTemplate.from_messages()` 公开 API 直接可达；
4. 命中的是多模态/tool_use 内容块的主力字段，非边缘路径。

## 4. 本轮产出

### 4.1 第 9 份诊断 · langchain-core#39152

**`scripts/repros/repro_39152_dict_prompt_list_scalar_drop.py`（新增）**
六臂对照，离线确定性，无 API key、无网络，验证于 langchain-core 1.5.3，退出码 0：

```
A  {"nums":[1,2,3],"flags":[True,None]}  -> {"nums":[],"flags":[]}            BUG
B  {"scalar":1,"nested":{"scalar":2}}    -> 原样保留                           OK  控制臂
C  {"payload":[["prefix","{q}"]]}        input_variables=[] 且 format() 不报错  BUG
D  {"payload":["{q}"]}                   input_variables=['q'] 且抛 KeyError    OK  控制臂
E  ChatPromptTemplate.from_messages(...)  -> {"nums":[]}                       BUG 公开API可达
F  {"nums":[1,2]}（零变量·mustache）      -> {"nums":[]}                       BUG 与渲染无关
```

A/B 是最小差分：唯一变量是容器类型。C/D 证明一层嵌套深度决定有没有安全网。
F 排除「渲染副作用」解释，钉死为无条件结构性丢弃。

**`docs/reports/ark-report-39152-20260731.html`（新增）** — 完整 4P 报告。

**根因（可离线验证）：** `_insert_input_variables()` 顶层 `else` 是白名单外**放行**，
list 内层循环是白名单外**丢弃**，相隔 4 行，对同一个 `1` 给出完全相反处置，且无任何
注释/文档/类型签名声明列表是有损的。同款遗漏在 `_get_input_variables()` 里**又出现一次**
——同一个 bug 在同一个文件里出现两次，与 F9 族 #39100/#39113 跨 provider 镜像遗漏同构。

### 4.2 处方回流自身代码库（第 2 例）

按 #39152 处方对 ARK 自审，`OutputValidator.validate()` **命中同型缺陷**：

```
payload = {order_id, amount, currency, idempotency_key, line_items}
r = v.validate(Order, payload)
r.valid   -> True
r.errors  -> []
r.data    -> {order_id, amount}
实际丢弃  -> currency, idempotency_key, line_items
给调用方的信号 -> 无
```

三个业务关键字段（含幂等键）静默蒸发，返回 `valid=True`。与 #39152 形状完全一致：
**过滤是设计意图，静默不是。**

**修复 v0.8.2 · 结构守恒不变式：**
- `ValidationResult.dropped_fields` 留痕（确定性排序，便于告警去重）
- `ValidationResult.lossless` 属性 + `to_dict()` 携带丢弃清单（跨进程边界不丢信息）
- `OutputValidator(strict_extra=True)` 把丢弃升级为契约违规，报错点名具体字段
- 新增 OTel 事件 `ark.validation.dropped`
- `stats` 新增 `dropped` / `drop_rate`
- 默认 `strict_extra=False`，向后完全兼容

**`tests/test_v0_8_2_validator_conservation.py`（新增，16 用例）** — A/B/C/D 四臂对照：
A（存在未声明字段）/ B（精确匹配）/ C（严格模式拦截）/ D（严格模式不误伤），
外加边界与兼容用例（类型错误路径、None 输出、直接构造默认值、OTel 枚举登记）。

### 4.3 副产物 · F4 族自身命中

修复过程中 `test_langfuse_demo.py` 的 `assert exporter._total_emitted == 8` 硬编码
把「新增事件类型」误报为回归失败。断言已改为对齐 `len(EventType)` 并加下限护栏
（`>= 9`，防枚举被误删后用例退化为空转）。这是 F4「测试即契约」族在自家代码上的
第 2 次命中——继上轮 `test_f9_trace_no_end` 把错误行为固化为正确契约之后。

## 5. 本轮动作清单

- 仓库状态核查（star / issue / PR / 分支同步 / 收付数据）
- 基线回归 265/265 全绿
- 上游 25 条 open issue 全量扫描 + #39163 进展跟踪
- 4 个候选 issue 本地实测，选定 #39152
- 六臂离线复现脚本 + 完整诊断报告
- ARK 自审 → 定位同型缺陷 → v0.8.2 修复 + 16 用例回归
- 修正被硬编码固化的旧断言
- 缺陷索引 v2.9 → v3.0 / ROADMAP 更新
- 巡航报告归档 + 提交推送

## 6. 异常

- 无技术异常。测试 265 → 281 全绿。
- **🟡 黄区阻塞（延续）：DEV.to 发布需账号授权。** 稿件 #1 就绪、`published: false`，
  环境中无可用 API Key。观一不擅自创建账号、不擅自发布。
- **战略层连续第 10 轮卡在 A/B/C 待决分叉。**

## 7. 战略判定

上一轮提出「诊断资产同时是对内审计清单」。本轮是该判断的**第二次独立验证**，
而且比上一轮更强——不是同一个族的复发，是**另一个族（F8）的处方对准自己同样命中**。

两轮数据：
| 轮次 | 上游诊断 | 对准自己 | 结果 |
|---|---|---|---|
| 15:28 | #39163（F3 终态缺失） | Trace | 命中 3 处缺口 |
| 17:28 | #39152（F8 无声删除） | OutputValidator | 命中 1 处，含幂等键泄漏 |

**两轮两中。** 这说明命中不是巧合，而是方法本身有效：
**任何一个成熟框架里的缺陷族，都大概率在你自己的代码里有一份拷贝。**

这条叙事对 C 案（内容/影响力路径）的价值远高于再写一份诊断——它把 ARK 从
「一个声称能防错的工具」变成「一个被自己的方法反复证伪又修好的工具」。
可信度的来源不是「我们没犯错」，而是「我们有能力发现自己犯的错」。

**观一倾向不变：C > B > A。**

## 8. ROADMAP 未完成项（C 案存量）

- [ ] **DEV.to 发布** ← 稿件 #1 就绪，卡账号授权（🟡 需主人一句话）
- [ ] 构建日志 #2 — **素材已足**：#39163 + #39152 双案，「处方对准自己两次都命中」
- [ ] GitHub Discussions 分发
- [ ] Cloud Dashboard (hosted version)
- [ ] 诊断管道自动化
- [ ] 付费诊断服务 MVP

## 9. 需主人决策（累计）

1. **A/B/C 战略分叉确认**（连续第 10 轮待复）
2. **DEV.to 账号**：是否授权创建 + 提供 API Key？稿件已就绪，给 key 即可发。

---

*ARK Cruise Bot · 2026-07-31 17:28–17:52 CST · 281 passed / 3 skipped*
