# ARK 守护型巡航报告 · 2026-08-03 11:28 CST

## 1. 巡航概要

| 项目 | 状态 |
|---|---|
| 仓库分支 | main，本地领先 1 commit（daily_status 时间戳待提交）|
| GitHub star / fork | 0 / 0（无变化）|
| Open issue | 1（#1 Governance layer for CrewAI Agent，非缺陷，无新增）|
| Open PR | 0（无新增）|
| 基线回归 | **300 passed / 3 skipped**（72.34s，全绿）✅ |
| 线上存在性（内容指纹）| 6/6 通过 ✅ |
| 可达性检查 | 31/31 全可达（depth1:9 / depth2:22）✅ |
| 埋点覆盖检查 | 31/31 全覆盖，track.js 引用 30 页 ✅ |
| 埋点数据 | daily_status: 0 new leads / 0 payments / 0 errors |
| 线上 API 实测 | `/api/track` `{ok:true,code:0}` ✅；`/api/stats` 返回完整聚合（page_views:130, real_visitors:100）✅；`/api/selfcheck` POST `{ok:true}` ✅；`/api/claim` 邮箱校验生效 ✅；`/api/waitlist` 正常写入 ✅ |
| 本轮推进 | daily_status.json 时间戳同步（巡航后提交）|
| 新发现 | 0 |

> 注：cron 指令中「9/9 通过」为历史遗留描述，测试套件已增长至 300/303（3 skipped 为环境相关，非缺陷）。

## 2. 各检查明细

### 2.1 基线回归（tests）
- `python3 -m pytest -v` → **300 passed, 3 skipped in 72.34s**，连续多轮全绿
- 与上轮（07:29）一致，无回归、无新增失败

### 2.2 GitHub 状态
- stars: 0 / forks: 0（与上轮一致）
- open issues: 1（#1，历史遗留非缺陷，0 comments 无新增）
- open PRs: 0

### 2.3 线上 API 实测（本轮补充验证）
| 端点 | 方法 | 结果 |
|---|---|---|
| `/api/track` | POST | `{"ok":true,"code":0}` → 根版（飞书 Bitable）线上生效 ✅ |
| `/api/stats` | GET | `{"ok":true,"page_views":130,"real_visitors":100,"selfchecks":2,...}` 含 pages 归因维度 ✅ |
| `/api/selfcheck` | POST | `{"ok":true,"event":"selfcheck_complete"}` ✅ |
| `/api/claim` | POST | `{"success":false,"error":"邮箱格式错误"}`（校验逻辑生效）✅ |
| `/api/waitlist` | POST | `{"ok":true,"record":"cruise-test@ark.dev"}` ✅ |
| `/api/verify` | GET | HTTP 200 ✅ |

> 更正：上轮巡航曾用 GET 探测 `/api/selfcheck` 得到 405/首页 HTML，本轮改用正确方法 POST 验证——**所有 API 函数线上均已生效**，无未部署函数。两套 track.js 中线上生效为根版（写飞书），ark-pro 版（写 CF KV）确认为死代码。

### 2.4 可达性 + 埋点
- 31/31 投放页从首页/README 传递可达（depth1:9 / depth2:22）
- 31/31 均有 page_view 上报 + page 归因维度（track.js 引用 30 页 + diagnose 内联）

### 2.5 SPA fallback 假绿复现
- `/nonsense-xyz` 假路径仍返回 HTTP 200 + 首页内容（size=12362，MD5 与首页一致 e8c6e2fa...）
- 结论：状态码探测仍不具备存在性证明能力；当前巡航已改用**内容指纹校验**（6/6 通过），不受影响

## 3. 待主人裁决项（无新增，沿用上轮）

1. **（待授权）DEV.to 发布**：构建日志 #1/#2 已定稿（`published: false`），仅差账号/API Key —— W33 P0，需主人提供 token
2. **（待裁决）双 `/api/track` 契约清理**：线上生效为根目录版（写飞书 Bitable，实测响应 `{ok,code}`）；`ark-pro/functions/api/track.js`（写 CF KV）确认为未部署死代码。推荐方案 A：删除 ark-pro 版 + 显著废弃标记。**删除文件属红区操作，需主人授权后执行**
3. **（待裁决）404 页 / `_routes.json`**：根治 SPA fallback 假绿（当前门禁已能检测，根治后可降级为保险丝）—— 修改部署配置属黄区操作

## 4. 下一步建议（按优先级）

1. 主人提供 DEV.to token → 发布构建日志 #1/#2（W33 P0）
2. 主人裁决双 track 清理方案
3. 主人裁决 404 根治方案
