# ARK 守护型巡航报告 · 2026-08-01 05:28 CST

## 1. 巡航概要

| 项目 | 状态 |
|---|---|
| 仓库分支 | main，与 origin 同步（0/0） |
| GitHub star | 0 · fork 0 · watcher 0 |
| Open issue | 1（#1，竞品 TrustLoop 推广 DM，非缺陷） |
| Open PR | 0 |
| 基线回归 | **300 passed / 3 skipped**（59.24s，全绿）|
| 收付数据 | new_leads 0 · new_payments 0 · errors [] |

> 备注：cron 指令里的「9/9 通过」为历史遗留描述，实际测试套件已随
> v0.8.1~v0.8.3 增长至 300/303，多轮全绿。

## 2. 本轮动作清单

- 仓库状态核查（分支同步 / star / issue / PR / 收付数据）——全部正常
- 基线回归：303 用例收集，300 passed / 3 skipped 全绿（59.24s）
- Issue #1 复核：仍为竞品推广私信转帖，非技术缺陷，无需修复
- **推进下一步（实质产出）：撰写并定稿 DEV.to 构建日志 #2**
  —— `docs/blog/2026-08-01_devto_buildlog-02_the-prescription-came-back-for-us.md`
  这是 Build Log #1 结尾明确承诺、且 W2 长期缺口中唯一无需授权即可推进的项。
- 同步更新 `docs/blog/index.md`（置顶 #2）+ ROADMAP W2 勾选构建日志 #2
- 巡航报告归档 + 提交推送

## 3. 构建日志 #2 内容要点（受众：DEV.to / 英文技术圈）

标题：*The Prescription Came Back For Us*。主线 = 「处方回流」闭环：

1. **#39163 全 autopsy**：`trace_as_chain_group` 用 `except Exception` 兜底，
   `CancelledError`/`KeyboardInterrupt`（继承 `BaseException`）绕过 on_chain_error
   与 on_chain_end → run 永久 pending，「失败伪装成无」。离线双臂对照
   （CancelledError 终态事件=0 / ValueError 控制臂=1）。处方写成不变式：
   每个 span 必达唯一终态 `{ok,error,cancelled,orphaned}`，且 cancelled 是合法
   终态而非失败（否则断连风暴打爆熔断）。
2. **回流命中自身，连续三例**：
   - v0.8.1 Trace 终态不变式（自身 summary 把未闭合 span 洗成 ok，且旧测试把
     错误行为固化为绿；248→265）
   - v0.8.2 OutputValidator 结构守恒（schema 外字段静默过滤仍报 valid=True；
     265→281）
   - v0.8.3 属性访问全函数（`getattr(x,"y")` 伪守护 + 真值当存在性；281→300）
3. **主题升华**：三类缺陷都是「只以约定形式存在的契约」，type checker / schema /
   APM 均抓不到；唯一解是「单检查点的运行时不变式」——正是 ARK 的核心论点。
4. front-matter `published: false`（不擅自发布，见 §5）。

## 4. 异常处理

- 无技术异常。测试全绿，仓库同步。
- **诚实备注（repro 可运行性）**：`scripts/repros/` 三个离线脚本按其头部说明
  需各自建 venv 安装 `langchain-core` / `exa_py` 后运行；base 环境未装这些
  第三方依赖属预期（脚本本就要求独立 venv）。为守成本上限，本轮未安装重依赖
  逐一跑通；真实回归门槛 300/303 已全绿，故不阻断。
- **🟡 黄区阻塞（延续）：DEV.to 发布仍需账号授权。** 稿件 #1、#2 均就绪、
  `published: false`，环境无 API Key。观一不擅自建号、不擅自发布。

## 5. 需主人决策（累计）

1. **A/B/C 战略分叉确认**（连续多轮待复）
2. **DEV.to 账号**：是否授权创建 + 提供 API Key？现已有 **两篇** 构建日志
   （#1 Diagnosed at Noon / #2 The Prescription Came Back For Us）待发，
   给 key 即可连发。

---

*ARK Cruise Bot · 2026-08-01 05:28 CST · 300 passed / 3 skipped · 本轮推进：Build Log #2 定稿*
