# ARK × Langfuse 演示 — 环境变量示例
# 复制为 .env 后填入真实值

# OpenTelemetry Collector 端点 (默认指向本机 docker 端口)
ARK_OTEL_ENDPOINT=http://localhost:4318/v1/traces

# Langfuse (默认本地,无需修改)
LANGFUSE_HOST=http://localhost:3000
